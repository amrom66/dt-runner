apiVersion: "apiextensions.k8s.io/v1beta1"
kind: "CustomResourceDefinition"
metadata:
  name: "model"
spec:
  group: "com.dtwave"
  version: "v1"
  scope: "Namespaced"
  names:
    plural: "models"
    singular: "model"
    kind: "Model"
  validation:
    openAPIV3Schema:
      required: ["spec"]
      properties:
        spec:
          required: ["replicas"]
          properties:
            replicas:
              type: "integer"
              minimum: 1