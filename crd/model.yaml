apiVersion: "apiextensions.k8s.io/v1"
kind: "CustomResourceDefinition"
metadata:
  name: "model.apps.dtwave.com"
spec:
  group: "apps.dtwave.com"
  names:
    plural: "models"
    singular: "model"
    kind: "Model"
  scope: "Namespaced"
  versions:
  - name: v1
    served: true
    storage: true
    scheme:
      openAPIV3Schema:
        type: object
        properties:
          spec:
            type: object
            properties:
              jobs:
                type: object
              archive:
                type: object